<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="MyPopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColor">#585858</item>
    </style>
    <style name="MyAppBarStyle" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="actionMenuTextColor">@color/colorWhite</item>
        <item name="android:textSize">14sp</item>
        <!--<item name="android:textColor">@android:color/holo_purple</item>-->
        <!--<item name="android:color">@android:color/holo_red_dark</item>-->
        <!--<item name="android:gestureColor">@color/teal</item>-->
        <!--android:textColorPrimary居然影响到了返回按钮与菜单按钮的颜色-->
        <item name="android:textColorPrimary">@color/colorWhite</item>

    </style>

    <style name="my_dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <!--设置dialog不全屏 -->
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <!--除去title，如果在代码里面有其他设置会没效果的不过完全没效果-->
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <!--<item name="android:windowBackground">@null</item><!–除去背景色–>-->
    </style>

    <style name="dialogstyle" parent="android:Animation">
        <item name="@android:windowEnterAnimation">@anim/dialog_enter</item>
        <item name="@android:windowExitAnimation">@anim/dialog_exit</item>
    </style>
</resources>